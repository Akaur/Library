Libraries in cpp

#### This library is used to draw line or circle.  #####

Download this library and place it where you want to use it.

If you don't have sudo powers,then use the following steps to execute this library.


1) Compilation
		  $ export LD_LIBRARY_PATH=/home/username/Library/liblc
		  $ echo $ LD_LIBRARY_PATH
		  If output is /home/username/Library/liblc ,then execute next command	
  		  $ g++ -L/home/username/Library/liblc -Wall -o test lcmain.cpp -llc

    
2) Execution

   		  $ ./test
