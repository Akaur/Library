Libraries in C++

This library is used to draw line or circle.

Two ways to execute this library

   #### Linking with your program ####

Download this library and place it where you want to use it.

If you don't have sudo powers,then use the following steps to execute this library.

	$ g++ -L/home/username/lc -Wall -o test lcmain.cpp -llc
	$ export LD_LIBRARY_PATH=/home/username/lc:$LD_LIBRARY_PATH

Then run this
	$ ./test


 #### Create and execute library  ####

$ git clone git://github.com/Akaur/Library.git

Go to this directory.eg:
$ cd home/username/Library

Then run make
$make
